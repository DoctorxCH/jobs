<?php

// Placeholder migration.
// Purpose: Create credit_reservations table.
// Fields: id, company_id, amount, purpose, reference_type, reference_id, status, expires_at, timestamps.
// Indexes: index(company_id), index(status), index(reference_type), index(reference_id).
// FKs: company_id -> companies.id.
// TODO: Replace with artisan-generated migration.
